---
import CharacterIsland from "./Character.tsx";

const images = Object.keys(
  import.meta.glob("/public/characters/*.png", {
    eager: true,
  }),
).map((path) => path.replace("/public", ""));
---

<CharacterIsland images={images} client:load />
